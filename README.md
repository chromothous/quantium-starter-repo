# Quantium starter repo
This repo contains everything you need to get started on the program! Good luck!

Running Tests

This project uses Dash Testing and Selenium for automated UI testing.

Requirements:

Google Chrome
Matching ChromeDriver version

To run the test suite:

python -m pytest -v

If you receive a ChromeDriver version error, ensure that your installed Chrome browser version matches your ChromeDriver version.
